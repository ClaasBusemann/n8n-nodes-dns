---
id: KNX-00007
title: RDATA record parsers for all supported types
epic: parsing
status: in_progress
priority: high
tags:
- dns
- parsing
- pure-function
depends_on:
- KNX-00002
refs:
- kontext/docs/n8n-dns-node.md
---

Implement RDATA parsers for all supported DNS record types in `src/utils/record-parsers.ts`.

## Context

From the design document, section **File Responsibilities**:

> **`record-parsers.ts`** — Pure functions. Parses RDATA bytes for each supported record type into structured objects. Registered in a type map, dispatched by TYPE field.

From section **Output Format > Record Type Value Formats**, the supported types and their output structures:

| Type | Value Format |
|------|-------------|
| A | `"93.184.216.34"` |
| AAAA | `"2606:2800:220:1:248:1893:25c8:1946"` |
| MX | `{ "priority": 10, "exchange": "mail.example.com" }` |
| TXT | `{ "raw": "v=spf1 include:...", "parsed": { ... } }` |
| SRV | `{ "priority": 10, "weight": 60, "port": 5060, "target": "sip.example.com" }` |
| SOA | `{ "mname": "...", "rname": "...", "serial": 2024010101, ... }` |
| NS | `"ns1.example.com"` |
| CNAME | `"alias.example.com"` |
| PTR | `"host.example.com"` |
| CAA | `{ "flags": 0, "tag": "issue", "value": "letsencrypt.org" }` |
| NAPTR | `{ "order": 10, "preference": 100, "flags": "s", "service": "SIP+D2U", ... }` |

Also DNSKEY and TLSA from the parameter list.

## Tasks

- Implement a parser for each record type: A, AAAA, MX, TXT, SRV, SOA, NS, CNAME, PTR, CAA, NAPTR, DNSKEY, TLSA
- Register parsers in a type map, dispatched by the TYPE field of each resource record
- Use name decompression from KNX-00002 for types containing domain names (MX, SRV, SOA, NS, CNAME, PTR, NAPTR)
- Write unit tests (TDD) per the **Testing Strategy > Unit Tests > record-parsers.ts** section:
  - Parse RDATA for each supported record type from known byte sequences
  - MX: priority extraction and exchange name decompression
  - SRV: all four fields (priority, weight, port, target)
  - TXT: multiple character strings within a single record
  - SOA: all seven fields
  - CAA: flags, tag length, tag, value
  - Malformed RDATA: truncated buffer, length mismatch — assert graceful error rather than crash

## Acceptance Criteria

- All 13 record types produce the value format specified in the design document
- Pure functions with no I/O or side effects
- Malformed RDATA produces graceful errors, not crashes
- All unit tests pass
