---
id: KNX-00010
title: DnsServerApi credentials definition
epic: nodes
status: in_progress
priority: medium
tags:
- n8n
- credentials
depends_on:
- KNX-00001
refs:
- kontext/docs/n8n-dns-node.md
---

Implement the DnsServerApi credentials in `src/credentials/DnsServerApi.credentials.ts`.

## Context

From the design document, section **Credentials**:

> The credential stores connection configuration for a reusable DNS server. The credential `name` is `dnsServerApi`, ending with `Api` as required by the n8n linter.

| Field | Type | Required | Description |
|---|---|---|---|
| `server` | string | yes | IP address or hostname of the DNS server |
| `port` | number | no | UDP port. Default: 53 |

> DNS has no authentication mechanism, so there is no token or password field. The credential exists purely for configuration reuse.

> There is no credential test block. Unlike HTTP APIs, there is no equivalent of `GET /health` for DNS.

From section **n8n Linter Compliance**:

> **`cred-class-field-name-missing-api`** — Credential `name` is `dnsServerApi`, ending with `Api`.

## Tasks

- Implement the credential class with `name: 'dnsServerApi'`
- Add `server` field (string, required) and `port` field (number, optional, default 53)
- No credential test block (DNS has no health check endpoint)
- Ensure linter compliance: name ends with `Api`

## Acceptance Criteria

- Credential definition is valid and loads in n8n
- Linter rule `cred-class-field-name-missing-api` passes
- No authentication fields (DNS has no auth)
